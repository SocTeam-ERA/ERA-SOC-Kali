{
  "rules": [
    {
      "id": "zeek-self-signed-internal",
      "reason": "Internal network devices use self-signed certificates; already reported to IT in the 2026-09-21 findings report (finding 8). Zeek re-reports every connection.",
      "added_by": "claude-code",
      "added": "2026-09-21",
      "expires": "2026-12-31",
      "match": {
        "detector": "zeek",
        "severity": [
          "normal",
          "medium"
        ],
        "title_contains": "SSL::Invalid_Server_Cert"
      }
    },
    {
      "id": "aide-project-bytecode",
      "reason": "Python regenerates __pycache__ bytecode whenever project code is imported or edited; source files are still checked and compared with git. Kept permanently (not interim): the deploy/sudo_tasks.sh exclusion applied 2026-09-21 stops AIDE from reporting the .pyc files themselves, but adding/removing them still bumps the mtime of the parent directory (kali/scripts/docs/config), which AIDE keeps tracking -- confirmed on the first check after the reinit (2026-09-22), which still found this and nothing else at these paths.",
      "added_by": "claude-code",
      "added": "2026-09-21",
      "expires": "2027-09-30",
      "allow_critical": true,
      "match": {
        "detector": "aide",
        "title_regex": "^File integrity: /opt/sentinel-soc(/(kali|scripts|docs|config))?(/__pycache__(/[A-Za-z0-9_.-]+\\.pyc)?)? (changed|appeared|was removed)$"
      }
    }
  ]
}

# /etc/sentinel-soc/ad-ldap.env  (root:soc, mode 640) -- read by scripts/ad_inventory.py
# The real file holds a password: keep it out of git and in the team's password vault.
AD_LDAP_SERVERS=10.69.0.14,10.69.0.15
AD_LDAP_USER=svc-soc-ldap@era.local
AD_LDAP_PASSWORD=
AD_BASE_DN=DC=era,DC=local
# AD_LDAP_MODE=ntlm
# AD_NTLM_DOMAIN=ERA
# AD_LDAP_CA_FILE=/path/ca.pem

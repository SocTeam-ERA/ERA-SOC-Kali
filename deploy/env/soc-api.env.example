# /etc/sentinel-soc/soc-api.env  (root, mode 600) -- EnvironmentFile of soc-api.service
# Copy the real values from the team's password vault. Settings soc_api.py reads:
# SOC_API_CORS=https://dashboard.example

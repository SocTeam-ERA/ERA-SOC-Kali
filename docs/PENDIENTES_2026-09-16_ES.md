# Pendientes — Sentinel // SOC · 2026-09-16

Lista acumulada durante la sesión de hoy, para retomar mañana.

**Corrección importante (confirmada con el usuario):** `https://soc.era.ca` es interno solamente — el túnel de Cloudflare resuelve dentro de la red/VPN de la oficina, pero a propósito **no** está pensado para acceso público. Se confirmó probando desde datos móviles (fuera de la red de oficina): no carga, como se espera. Si en algún momento alguien externo necesita verlo, la vía es una VPN, no un hostname público. Los puntos de "antes de exponer públicamente" de abajo quedan en espera hasta que eso cambie — no son urgentes para mañana.

## Acceso externo para managers (futuro, no urgente)
- [ ] Cuando algún manager necesite ver el dashboard desde fuera de la oficina, la vía decidida es VPN (no exponerlo públicamente). Falta elegir/armar esa VPN y dar de alta a los managers que la necesiten.

## Si algún día se decide exponerlo fuera de la VPN (no urgente por ahora)
- [ ] Confirmar que los endpoints de escritura (`POST /api/alerts/<id>/status`, `POST /api/assets/<mac>/notes`) exigen key `write` sin excepción, y que nada queda accesible sin auth por depender de estar en red interna. (El código de `soc_api.py` ya lo hace bien — solo confirmarlo de nuevo tras cualquier cambio antes de exponer.)
- [ ] Decidir cómo va a autenticarse el dashboard contra la API sin exponer una key de escritura en el JS público (cualquiera con devtools la vería si se hace ingenuamente).
- [ ] Configurar `SOC_API_CORS` si el dashboard y la API terminan sirviéndose desde orígenes distintos detrás del túnel.

## Dashboard desconectado del backend real
- [x] **Confirmado 2026-09-17: ya no aplica.** Sixto construyó un frontend nuevo (SvelteKit, con login, compilado vía dokploy en el Ubuntu server, publicado en `soc.era.ca`) que sí habla con la API real (`soc_api.py`, puerto 8080) — se verificó end-to-end simulando 13 alertas distintas (una por cada detector: login_monitor, arp, malware, phishing, nmap, port_scanner, nikto, whatweb, vlan_segmentation, traffic_capture, suricata, zeek, osquery) y las 13 aparecieron correctamente en `soc.era.ca` tras iniciar sesión. El viejo `dashboard/sentinel_soc.html` (el que solo leía el archivo estático) quedó obsoleto/reemplazado por este nuevo frontend.
- [ ] Falta confirmar con Sixto: si Acknowledge/Resolve ya persisten de verdad a través del nuevo frontend (llaman a `POST /api/alerts/<id>/status`), o si ese detalle sigue pendiente del lado de él.
- [x] **Corrección 2026-09-17: esto estaba mal dicho.** `dashboard/sentinel_soc.html` SÍ tiene una sección "Asset Inventory" completa (panel + JS) que lee `data/assets.json` y coincide exactamente con el esquema de `soc_core.py` — se verificó leyendo el archivo completo. No falta construir nada aquí. Lo único por confirmar con Sixto: si su nuevo frontend en SvelteKit ya incluye esta misma pantalla o si esa parte todavía no la portó.
- [ ] Confirmar con Sixto cómo maneja la autenticación del nuevo frontend hacia la API (login propio + proxy del lado servidor, o algo distinto) — esto puede ya resolver el punto de "no exponer una key de escritura en el JS público" de la sección de abajo.

## Enriquecimiento de inventario de activos
- [ ] Nuevo parser (al estilo `arp_to_alerts.py`) que lea `dhcp.log` y `http.log` de Zeek para enriquecer cada MAC nueva con el tipo de dispositivo probable (vendor class de DHCP, User-Agent de HTTP), sin depender de correr `nmap -O` manualmente. Surgió de la alerta de hoy de un dispositivo nuevo en la VLAN de oficina (`00:1b:82:71:9d:e3`, vendor "Taiwan Semiconductor Co., Ltd.") donde solo se pudo identificar el fabricante del chip, no el modelo/tipo real.

## Administración
- [ ] Crear los usuarios/logins nuevos del equipo cuando el túnel esté arriba.
- [ ] Revisar y decidir sobre el cambio sin commitear en `kali/traffic_to_alerts.py` (+8 líneas, sin relación con lo anterior).
- [ ] Repaso de punta a punta (Kali, backend, frontend) una vez que `soc.era.ca` esté funcionando, antes de darlo por cerrado.

## 2026-09-18 — AIDE era el mayor generador de ruido de todo el proyecto
- [x] **Resuelto.** Encontrado por el usuario: el dashboard se estaba llenando de alertas "File integrity: ... changed" sin sentido. Investigación reveló que **AIDE representa 6,434 de ~11,367 alertas en todo el histórico del proyecto (~57% de TODO el ruido jamás generado)**, muy por encima de `kali_scan` (4,260). La causa real: la exclusión `!/var/lib/suricata/cache` ya existía en `/etc/aide/aide.conf.d/90_sentinel_soc` (agregada por el compañero el mismo día), pero la base de datos de AIDE seguía rastreando esos archivos de antes de que se agregara la exclusión — un cambio de config no purga retroactivamente lo ya rastreado, hace falta `aide --init` completo.
- Se limpiaron 499 alertas de ruido del snapshot y se corrió `aide --init` dos veces (primero para `suricata/cache`, luego otra vez tras agregar `!/usr/share/applications`, ~29 min cada una). Confirmado: `aide --init` en este sistema escribe directo a `aide.db` (ignora `database_out`/`database_new` de la config) — comportamiento real observado dos veces, no una condición de carrera.
- Se agregó nueva exclusión: `!/usr/share/applications` (accesos directos de menú de escritorio de Kali, churn genuino por regeneración de caché de menús, sin valor de seguridad).
- **Sin excluir a propósito** (pendiente de confirmar el lunes si reaparecen): `/opt/zeek/share` (2,022 hist.), `/opt/zeek/include` (1,543 hist.), `/opt/microsoft/powershell` (725 hist.). Sus fechas reales son de 2015 (archivos de instalación estáticos) — el volumen histórico probablemente vino del mismo bug de "re-alerta para siempre" ya arreglado hoy, no de cambios reales recurrentes. Se decidió NO excluirlos de entrada porque son archivos del propio Zeek/PowerShell — si alguien los modificara de verdad (para cegar la detección), sí queremos enterarnos. Verificar el lunes con: `python3 -c "import json; d=json.load(open('/opt/sentinel-soc/data/alerts.json')); print(sum(1 for a in d if 'suricata/cache' in str(a) or 'usr/share/applications' in str(a)))"` (debe dar 0) y revisar si `/opt/zeek/share`, `/opt/zeek/include`, `/opt/microsoft/powershell` reaparecieron.

## 2026-09-18 — rastreo de puertos cambiado de IP a MAC (cambio grande)
- [x] **Hecho, probado con datos sintéticos, pendiente de verificar con el próximo scan real.** `nmap_to_alerts.py` ahora rastrea el historial de puertos por dirección MAC (vía `data/assets.json`) en vez de por IP — en una red con DHCP, la misma laptop con IP nueva se veía como "host nunca antes visto" (todos sus puertos normales se marcaban "nuevos"). Se probaron los 3 escenarios clave (dispositivo conocido con IP nueva, host sin MAC resoluble, IP vieja reasignada a otro equipo) — todos correctos.
- ~~El cambio usaba un archivo de estado nuevo (`data/port_state_v2.json`)~~ — **revertido el 2026-09-18 por la avalancha descrita al final de este documento**: el script programado vuelve a usar `data/port_state.json`, que ya está en esquema por MAC.
- [ ] Confirmar en unos días que el volumen de "NEW open port" bajó de verdad comparado con los ~356 de los últimos 2 días antes del cambio.
- [x] **Revisado de nuevo (2026-09-22), ya no aplica podar nada:** hoy `port_state.json` tiene 66 entradas con llave IP pelada (no ~337) y `udp_state.json` una cantidad comparable; las 704 y 615 entradas totales de cada archivo, sin excepción, se actualizaron en los últimos 3 días — la retención de 14 días (`SEEN_RETENTION_DAYS`) ya las mantiene acotadas sola. Las llave-IP no son sobrantes muertas: son equipos que el escaneo sigue viendo cada 4h pero sin MAC resoluble (probablemente en subredes sin visibilidad ARP directa desde este Kali). No se tocó el estado en vivo.

## Aparte — investigación de los cortes de SSH
No es de este proyecto de código, pero queda documentado: hoy se cayó varias veces la sesión SSH hacia `kali2` con paquetes TCP RST fabricados desde el propio Kali hacia el cliente. Se descartó Suricata, fail2ban, los timers propios del proyecto, memoria/OOM y CPU steal time. La hipótesis que queda en pie (sin poder confirmarse desde esta VM) es un loop de red a nivel del switch virtual de Proxmox — pendiente de escalar a quien administre esa capa si vuelve a pasar. Mientras tanto: `tcpdump` sigue capturando evidencia en `/home/adelcueto/ssh_diag/`, y `sysstat`/acceso a `journalctl -k` (grupo `adm`) ya quedaron habilitados hoy para diagnosticar mejor la próxima vez. No volvió a pasar desde las 14:44 del 16 de septiembre, pero la causa raíz nunca se confirmó — sigue abierto.

## 2026-09-17 — sesión de limpieza de alertas + hallazgos nuevos

**🔴 Prioridad máxima — sin escalar todavía a IT/red:** el test `5_vlan_segmentation_test.sh` (metodología validada: fuerza cada prueba por el gateway real de la VLAN origen, no por el atajo de este Kali multi-homed) confirmó **30 de 30 pares de VLANs alcanzables — cero segmentación real entre ninguna de las 6 VLANs**, incluyendo Guest/Employee WiFi llegando directo a Management y Wiping. Reporte ya armado y pasado a Alberto para IT (ver artifact "VLAN Segmentation Finding"). Ahora corre solo todas las noches a la 01:00 (`soc-vlan-segmentation.timer`) para detectar cuando se arregle.
- [ ] Escalar el reporte a IT/equipo de red y darle seguimiento.

**Limpieza de ruido/falsos positivos en el feed de alertas (todo comiteado):**
- [x] Puertos cerrados ya no generan alerta (solo log) — `nmap_to_alerts.py`.
- [x] Wording de "RESOLVED" corregido a "No longer detected (unconfirmed)" cuando un hallazgo deja de verse — `nmap_to_alerts.py`.
- [x] 23 alertas de datos de prueba/demo (root desde nodo Tor, etc.) que se colaron en producción, limpiadas del snapshot. Causa: `--demo` de varios scripts no tenía el mismo guard que `seed_demo_data.py` — ya se agregó `confirm_demo_on_live_instance()` compartido a los 4 scripts con modo demo.
- [x] Falso positivo de Suricata: el refresh diario de la lista de nodos Tor (`proxy_check.py --refresh-tor`, este mismo equipo) se detectaba a sí mismo como "uso de Tor". Envuelto en `kali/refresh_tor.sh` con el marcador `mark_scan_start`/`scan_active()` que ya usan los demás scripts.
- [x] `traffic_to_alerts.py`: tráfico loopback (127.0.0.1→127.0.0.1) ya no genera falsa alerta de "cleartext protocol" (mismo patrón que ya existía para port-scan).
- [x] Severidad de "nuevo dispositivo" en la VLAN Wiping bajada de critical a medium — confirmado con el usuario que es un rack de borrado de HDD/NVMe/SAS todavía en construcción, con alta rotación de equipos esperada.
- [x] `zeek_to_alerts.py`: `CaptureLoss::Too_Much_Loss` agregado a la lista de ruido operativo de Zeek (mismo trato que los otros 2 tipos de CaptureLoss ya excluidos).

**Zeek / `eth0` (VLAN Floor) — capacidad de captura:**
- [x] Se intentó arreglar con más procesos worker (`lb_procs` 1→2→3 en `/opt/zeek/etc/node.cfg`) — **no funcionó**, la pérdida volvió a pasar de ~100% en cada intento, y el uso de CPU de cada worker se mantuvo bajo 1% (descarta que sea falta de capacidad).
- [x] Diagnóstico corregido: `CaptureLoss::Too_Much_Loss` mide huecos en secuencias/ACKs de TCP, no una cola de captura saturada — apunta a que `eth0` no está viendo tráfico bidireccional completo de otros hosts, es decir, no es un puerto espejo/SPAN real a nivel del vswitch de Proxmox. Misma familia de problema que la investigación de los cortes de SSH, en la misma interfaz. No se puede arreglar desde este proyecto.
- [ ] Si alguien con acceso a Proxmox revisa la configuración de red, este es el otro punto (junto con el loop de SSH) para preguntarle.

**Nueva cobertura de detección:**
- [x] `chkrootkit` (corría diario, nunca llegaba al dashboard) ya integrado vía `chkrootkit_to_alerts.py` + `soc-chkrootkit-forwarder.timer` (00:30 diario), con filtrado de los 3 falsos positivos esperados en este equipo (dotfiles de gems de `dradis`, scripts de `/tmp/claude-*`, sniffers legítimos de Zeek/Suricata en modo promiscuo).
- [x] Scan profundo de vulnerabilidades (`3_vuln_scan.sh`: nikto, whatweb, categoría `vuln` completa de nmap, SNMP) ya programado semanal (domingo 02:00, `soc-vuln-scan.timer`), reutilizando la lista de hosts ya filtrada sin Guest WiFi del scan regular — antes solo corría manual y llevaba 2 días sin correr.
- [x] `lynis` (también corre diario, tampoco llegaba al dashboard): decisión de **no** conectarlo como alertas (son ~29 sugerencias de hardening que casi no cambian día a día — meterlas todas como alerta recrearía el mismo problema de ruido que se pasó todo el día arreglando). En su lugar, se entregó la lista ya filtrada como backlog de hardening:
  - [ ] SSH: lynis sugiere mover el puerto 22 a uno no estándar (seguridad por oscuridad, bajo valor real) — decisión pendiente del usuario, requiere avisar a todos los que se conectan.
  - [x] **Explicado (2026-09-22), no es un problema:** `1.1.1.1` responde a `ping` pero la consulta DNS (puerto 53) se cuelga; igual pasa con `8.8.8.8`, mientras que nuestros resolvedores internos (`10.69.0.14`) contestan en 12 ms. `ufw status verbose` no tiene ninguna regla de salida que lo explique (política por omisión: salida permitida). El bloqueo está más arriba, en el firewall de la VLAN Floor (por donde sale la ruta por omisión, `eth0` → `10.69.0.1`): DNS externo bloqueado a propósito, solo se permite hacia los resolvedores internos autorizados. Es buena higiene de red (evita fuga de datos o túneles por DNS), no un fallo — Lynis solo no lo sabe.
  - [ ] Evaluar quitar TFTP si no se usa (`INSE-8318`/`INSE-8320`).
  - [ ] Revisar permisos de directorios home (`HOME-9304`).
  - [ ] No hay logging a un servidor externo — si este Kali se cae, se pierden los logs con él (`LOGG-2154`).
- [x] `soc_doctor.py`/`service_watchdog.py` actualizados para vigilar los 3 timers/servicios nuevos de hoy (`soc-chkrootkit-forwarder`, `soc-vuln-scan`, `soc-vlan-segmentation`, `soc-dhcp-fingerprint`).

**Revisión de seguridad de `soc_api.py`:** todo bien salvo un hallazgo, ya arreglado — no había límite de tamaño en el cuerpo de las peticiones POST (riesgo de agotar memoria), ahora limitado a 64KB. Firewall (`ufw`) ya restringe el puerto 8080 solo al backend y la VPN admin — confirmado, no depende únicamente de la API.

**Sin resolver / bloqueado por infraestructura que no existe todavía:**
- [ ] `malware_detector.py`: no hay carpeta compartida que vigilar. Sin acción por ahora.
- [ ] `phishing_detector.py`: no hay fuente de correo todavía — esperando a IT.

**Housekeeping menor, sin acción tomada:**
- [x] **Resuelto 2026-09-17:** la API key en texto plano en `~/.bash_history` (`[old key removed]`) ya no es la key activa — el usuario confirma que se rotó la semana pasada y Tomás ya tiene la key nueva en su backend. Se verificó que la key `legacy` actualmente en uso no coincide con el string expuesto. Sin acción adicional.
- [ ] Hay dos copias completas y viejas del proyecto en el home del usuario (`~/soc-project-new/`, `~/Desktop/soc-project/`), aparte de la de producción en `/opt/sentinel-soc` — desorden, no se tocaron.

## 2026-09-18 (tarde) — marca de escaneo pegada: Suricata/Zeek silenciados ~21 horas

- [x] **Bug encontrado por `soc_doctor.py`:** `5_vlan_segmentation_test.sh` reemplazaba el trap de salida de `mark_scan_start` (línea `trap cleanup_route EXIT`) y luego lo borraba (`trap - EXIT`), así que `mark_scan_end` nunca corría y la línea del escaneo quedaba en `data/scan_in_progress` para siempre. Mientras hay una línea ahí, `scan_active()` devuelve verdadero y se suprimen las alertas de Suricata/Zeek. Dos corridas (2026-09-17 15:08 y 2026-09-18 01:02, la del timer nocturno) dejaron su línea pegada, así que desde el 17 a las 15:08 hasta hoy ~12:45 esas alertas estuvieron silenciadas (no el resto del pipeline).
- [x] Arreglo: el trap ahora encadena ambas limpiezas (`cleanup_route; mark_scan_end`) y al final se restaura `trap mark_scan_end EXIT` en lugar de borrarlo. Las 2 líneas muertas se limpiaron a mano bajo el mismo candado. Se verificará con la corrida nocturna de mañana (01:00): `soc_doctor.py` no debe reportar líneas pegadas.
- [x] Falso positivo de `soc_doctor.py`: `os.kill(pid, 0)` da `PermissionError` con procesos de root (el escaneo programado) y se contaba como "muerto". Ahora se cuenta como vivo.
- [x] Endurecimiento hecho: `scan_active()` (`soc_core.py`, usado por los reenviadores de Suricata y Zeek) y el monitor de tráfico (`4b_traffic_monitor.sh`, vía la nueva `scan_marker_active` en `lib.sh`) ahora ignoran líneas de la marca cuyo PID ya no existe; un PID de root vivo (escaneo programado) cuenta como activo. Probado con marca vacía, solo espacios, PID muerto, PID propio, PID de root, mezcla de muerto+vivo y línea mal formada. **Requiere reiniciar** `soc-zeek-forwarder`, `soc-suricata-forwarder` y `soc-traffic-monitor` para que los procesos largos carguen el código nuevo. Límite conocido: si el sistema reutiliza el PID de un escaneo muerto para otro proceso, esa línea parecería viva.
- [x] Ver la incidencia siguiente: `port_state_v2.json` nunca se creó y no se usará.


## 2026-09-18 (tarde) — avalancha de 1.041 alertas falsas "NEW open port" tras el cambio a MAC

- **Qué pasó:** a las 13:16 (hora local) el escaneo programado emitió **1.041 alertas `NEW open port`** en ~20 segundos (316 críticas, 715 medias, 10 normales), más 86 de `161/udp`. Todas son falsas: los mismos puertos de siempre, vistos como "nuevos" porque el estado previo estaba en esquema por IP y el código nuevo busca por MAC.
- **Causa 1 (error mío, de diseño):** el cambio de nombre a `port_state_v2.json` solo cubría el escaneo TCP. El barrido UDP (`2_port_service_scan.sh`, `udp_state.json`) y los estados de vulnerabilidades (nombre derivado del archivo de estado) siguieron usando los archivos viejos con el esquema viejo, así que esa parte se inundó de todos modos.
- **Causa 2 (error mío, de proceso):** edité `scheduled_scan.sh` a las 12:09, con el ciclo de 12:03 ya corriendo. Bash lee los scripts de forma incremental, así que ese ciclo siguió usando `port_state.json` con el código Python nuevo, en lugar de sembrar `v2` en silencio. Regla: **no editar un script mientras su proceso está corriendo** (o reemplazarlo con archivo nuevo, no en sitio).
- **Estado actual:** el ciclo dejó `port_state.json`/`udp_state.json` ya en esquema por MAC. Se comprobó con un dry-run (emisión de alertas simulada) que repetir el mismo escaneo genera **0 alertas**, y se revirtió `scheduled_scan.sh` a `port_state.json`. El escaneo de las 16:02 debería ser silencioso.
- [x] **Limpieza hecha (decisión del usuario):** 1.026 de las 1.041 alertas se marcaron `resolved` con nota (`actor: claude-code`, registradas en `alert_status_log.jsonl`); se comprobó una por una que ese puerto ya estaba abierto en el estado anterior de esa IP. Las **15 restantes se dejaron abiertas** porque no se pudieron confirmar (hosts sin historial previo): `10.201.4.35`, `10.201.4.64`, `10.69.11.153`/`.74` (135/tcp, críticas), `10.69.14.177`, `192.168.61.2` y `192.168.61.250` (Apple AirTunes, 6 puertos). Revisar a mano. Las copias en el backend de Tomás no se tocaron; las 316 críticas pudieron generar un push de ntfy en lote.
- [ ] Verificar tras el escaneo de las 16:02 que salen ~0 alertas `NEW open port`.

## 2026-09-18 (tarde) — revisión de las 15 alertas abiertas
- [x] Resueltas como ruido (mismos puertos vistos en escaneos anteriores): `10.201.4.35` 80/443 tcp, `10.201.4.64` 22/tcp y 161/udp, `10.69.14.177` 62078/tcp, `192.168.61.2` 123/udp.
- [ ] **Pendientes de confirmar con IT/equipo:** `192.168.61.250` (Hikvision, 6 puertos de cámara IP, nueva hoy en la VLAN de impresoras); `10.69.11.153` (Dell) y `10.69.11.74` (Intel), Windows 11 con 135/tcp; `10.201.4.35` 161/udp (primera vez que aparece SNMP ahí; revisar comunidad SNMP en el escaneo profundo del domingo). Solo las 6 de la cámara siguen visibles en el dashboard; las otras 3 salieron del snapshot por el tope de 500 y solo existen en `alerts.jsonl`.
- [x] **Incidente menor:** las resoluciones hechas a las 13:39 se perdieron del snapshot (`alert_status_log.jsonl` las conserva) — alguien reescribió `alerts.json` con una copia anterior. Se reaplicaron (491 resueltas). Causa confirmada por la otra sesión: varios scripts de archivado de un solo uso (ruido de AIDE, duplicados de segmentación VLAN, alertas de traffic_capture, dedup de vulnerabilidades) reconstruían `alerts.json` desde `alerts.jsonl`, que no guarda los cambios de estado, y dejaban todo en `open`; dos de esas corridas cayeron entre las 14:20 y las 14:40. Regla: cualquier script que reconstruya el snapshot debe conservar `status`, `status_updated` y `status_note` de las alertas que sobreviven. Verificado: las 491 resoluciones siguen intactas (0 diferencias contra `alert_status_log.jsonl`).
- [x] **Causa de fondo arreglada:** el tope del snapshot era 5000 en los servicios de escaneo y 500 en el resto, y la siguiente alerta de otro detector recortaba lo que la ráfaga acababa de agregar. Ahora el valor por defecto en `soc_core.py` es 5000 para todos (los `Environment=SOC_MAX_SNAPSHOT=5000` de las unidades systemd quedaron redundantes pero inofensivos). Requiere reiniciar los servicios de larga duración (`soc-zeek-forwarder`, `soc-suricata-forwarder`, `soc-osquery-forwarder`, `soc-dhcp-fingerprint`, `soc-traffic-monitor`); las unidades `oneshot` lanzan procesos nuevos y toman el valor solas. Costo: `alerts.json` puede crecer hasta ~5,6 MB y cada alerta reescribe el archivo completo.

## 2026-09-18 (tarde) — etiquetas MITRE ATT&CK y archivo de supresiones
- [x] **Etiquetas MITRE** (`scripts/mitre_tags.py`): `emit_alert()` agrega `details.mitre` a cada alerta con una lista de `{technique, name, tactics, basis}`. `basis` es `observed` (la alerta es evidencia directa de la técnica: fuerza bruta, escaneo visto en la red, dispositivo intruso, etc.) o `exposure` (solo señala una superficie de ataque, por ejemplo un puerto RDP recién abierto). Solo se etiquetan mapeos defendibles: sin equivalente honesto (certificados vencidos, pérdida de captura, brechas de VLAN, disco) no hay etiqueta. Probado contra las 11.594 alertas históricas. Va dentro de `details`, no como campo nuevo de primer nivel, para no romper el esquema que valida el backend de Tomás; si él confirma que acepta campos extra, se puede subir un nivel. **Para Sixto:** leer `details.mitre` (puede no existir).
- [x] **Archivo de supresiones** (`config/suppressions.json`, versionado en git; el formato y los criterios están en el docstring de `scripts/suppressions.py`): una regla oculta una alerta conocida y benigna sin tocar código. Una alerta suprimida no llega al dashboard, al backend ni a ntfy, y no entra en `alerts.jsonl`, pero queda en `data/alerts_suppressed.jsonl` con `suppressed_by`. Salvaguardas: cada regla exige `reason`; una regla con clave desconocida, regex/fecha/CIDR inválidos, id repetido o `match` vacío se ignora (un error de tipeo nunca amplía una regla); un archivo roto no suprime nada; las alertas críticas solo se ocultan con `allow_critical: true`; `expires` (YYYY-MM-DD) hace que la regla caduque sola. `python3 scripts/suppressions.py --check` valida el archivo y `--dry-run` muestra cuántas alertas históricas ocultaría cada regla antes de activarla. `soc_doctor` avisa de reglas inválidas o vencidas y cuántas alertas ocultó cada una en 24 h. El archivo real está vacío a propósito.
- [ ] Requiere reiniciar los servicios de larga duración para cargar el código nuevo: `soc-zeek-forwarder`, `soc-suricata-forwarder`, `soc-osquery-forwarder`, `soc-dhcp-fingerprint`, `soc-traffic-monitor` y `soc-login`.
- [ ] Idea pendiente: una pantalla en el dashboard (de Sixto) para crear una supresión desde una alerta ("esto es normal") en vez de editar el archivo; requeriría un endpoint de escritura en `soc_api.py`.

## 2026-09-18 (noche) — lado Kali de la hoja de ruta tipo Splunk/Sentinel
**Qué se construyó** (todo probado en directorio temporal y con el historial real; sin commitear):
- [x] **Campos comunes** (`scripts/alert_context.py`), dentro de `details` en cada alerta: `entities` (`[{type: ip|mac|host|user, value, role: source|destination}]`; las IP se enlazan con su MAC por el inventario), `group_key` (tipo de alerta + zona) y `batch_id` (ráfaga: mismo `group_key` con menos de 300 s entre alertas). Las 17 alertas de dispositivos nuevos de hoy quedaron en 4 lotes, uno por VLAN.
- [x] **Inteligencia de amenazas** (`scripts/threat_intel.py`): CISA KEV y Feodo Tracker se descargan a diario (dentro de `refresh_tor.sh`) a `data/threat_intel/`; una coincidencia agrega `details.threat_intel` (`ip_matches` y/o `kev`). Nunca cambia la severidad. El monitor de tráfico usa la lista fusionada de IOCs.
- [x] **Correlación e incidentes** (`scripts/correlate.py`, reglas en `config/correlation_rules.json`): 6 reglas (escaneo→exploit, fuerza bruta→login, dispositivo nuevo→escaneo, 3 detectores distintos sobre el mismo equipo, IP maliciosa, CVE explotado). Abrir un incidente genera además una alerta normal (`detector: correlation`), así que llega al dashboard, al backend y a ntfy. Las IP propias de este Kali nunca correlacionan. Incidentes en `data/incidents.json`, bitácora en `data/incident_log.jsonl`. `python3 scripts/correlate.py --replay` muestra qué incidentes habría abierto el historial (hoy: solo los de datos de prueba).
- [x] **Salud de fuentes** (`scripts/source_health.py`, lo ejecuta el watchdog cada 2 min): Suricata, Zeek, escaneo programado, AIDE, chkrootkit, segmentación VLAN y feeds. Una fuente silenciosa genera una alerta y otra al recuperarse. Zeek sale "desconocido" por permisos, sin alertar.
- [x] **Riesgo por entidad, métricas y catálogo** (`scripts/soc_views.py`). El riesgo suma los puntos de las alertas sin resolver (crítica 20, media 5, con vida media de 3 días) más un bono por incidente abierto, tope 100. Hoy sale primero la cámara Hikvision y luego las dos laptops Windows: justo lo que estaba pendiente de confirmar.
- [x] **Nuevos endpoints de `soc_api.py`** (llave de lectura salvo el POST): `GET /api/incidents[?status=&severity=&limit=]`, `GET /api/incidents/<id|número>` (con las alertas incluidas), `GET /api/entities[?type=&limit=]`, `GET /api/entities/<tipo:valor>` (acepta IP o MAC), `GET /api/metrics`, `GET /api/sources`, `GET /api/detections` (detectores, cobertura MITRE por táctica, reglas de correlación y de supresión). `POST /api/incidents/<id|número>` (llave de escritura) con `status` (new|active|closed), `classification` (true_positive|false_positive|benign|undetermined; obligatoria para cerrar), `owner` y `comment`; el autor siempre es el usuario de la llave.
- [x] `soc_doctor` valida reglas de correlación, feeds, salud de fuentes e incidentes.

**Requiere reiniciar** para cargar el código nuevo: `soc-api`, `soc-zeek-forwarder`, `soc-suricata-forwarder`, `soc-osquery-forwarder`, `soc-dhcp-fingerprint`, `soc-traffic-monitor` y `soc-login`. Las unidades `oneshot` (escaneos, AIDE...) y el watchdog ya usan el código nuevo por sí solos.

**Para Sixto (frontend):** leer `details.entities`, `details.group_key`, `details.batch_id`, `details.mitre`, `details.threat_intel` e `details.incident_id` (todos opcionales; las alertas anteriores a hoy no los tienen); pantallas nuevas posibles: cola y detalle de incidentes, página por host o usuario, métricas, salud de fuentes, cobertura MITRE. Los comentarios y el dueño de un incidente son texto libre: hay que escaparlos al mostrarlos.
**Para Tomás (backend):** confirmar que acepta `details` con objetos y listas anidadas (`entities`, `mitre`, `threat_intel`); si valida el esquema de `details` de forma estricta rechazaría las alertas nuevas. Decidir si él quiere ser dueño del estado de los incidentes (hoy viven en Kali y se exponen por `soc_api.py`, con lo que el frontend actual ya puede usarlos).
- [ ] Pendiente futuro: crear supresiones desde el dashboard (requiere un endpoint de escritura que edite `config/suppressions.json`); búsqueda sobre logs crudos de Zeek/Suricata; matriz de cobertura MITRE con datos de más detectores.

## 2026-09-18 (noche) — supresiones desde el dashboard, búsqueda sobre datos crudos y automatización
Sin commitear al momento de escribir esto. Probado en directorio temporal, con un servidor de API real y con datos reales de Zeek y Suricata.
- [x] **Supresiones desde el dashboard** (`scripts/suppression_admin.py`, reglas en `data/suppressions_dashboard.json`, fuera de git; las reglas versionadas siguen en `config/suppressions.json` y solo se editan a mano). Endpoints: `GET /api/suppressions`, `POST /api/suppressions/preview`, `POST /api/suppressions` y `DELETE /api/suppressions/<id>` (los tres últimos con llave de escritura; el autor es siempre el usuario de la llave). Se crea desde una alerta con `{"alert_id", "scope": "similar|host|broad", "reason", "expires_days", "resolve_existing"}`: `similar` (por defecto) oculta el mismo título con cualquier IP/MAC y los números (puertos) exactos; `host` lo limita a la IP de origen; `broad` oculta todo el mismo tipo de alerta de ese detector y **grupo** (ahí los números se borran, así que un puerto representa a todos: la vista previa lo muestra). Salvaguardas: siempre caduca (30 días por defecto, máximo 365), nunca oculta alertas críticas (eso solo con `allow_critical` en el archivo versionado), exige detector más otro criterio, el cliente nunca envía expresiones regulares, máximo 200 reglas, y cada alta o baja queda en `data/suppressions_log.jsonl`. `resolve_existing` marca como resueltas las alertas abiertas que ya coinciden.
  - Se encontró y corrigió durante las pruebas que el `group_key` (que borra números) era demasiado grueso para suprimir: ocultar el puerto 62078 también ocultaba el 3389. Por eso existe el criterio `title_template` (solo generaliza IP y MAC).
- [x] **Búsqueda** (`scripts/log_search.py`): `GET /api/search?source=&q=&since=&limit=&timeout=` y `GET /api/search/sources`. Fuentes: `alerts`, `suricata` y `zeek:<log>` (conn, dns, http, ssl, notice...; incluye los archivos horarios comprimidos). Sintaxis: palabras o "frases", `campo:valor`, `campo~texto`, `src:10.0.0.0/8`, `-término`; atajos `src dst sport dport ip host type sig`. Acotada: más reciente primero, máximo 500 resultados, 15 s y 400 MB leídos por búsqueda, ventana de hasta 7 días; se ejecuta en un proceso hijo con límites de CPU y memoria y como máximo 2 a la vez (la tercera recibe 429). No acepta expresiones regulares. Desde terminal: `python3 scripts/log_search.py --source zeek:dns --q 'query~ejemplo.com' --since 6h`.
- [x] **Automatización** (`scripts/playbooks.py`, `config/playbooks.json`): 3 playbooks de ejemplo, **todos desactivados** (`enabled: false`). Acciones: `webhook`, `ntfy`, `assign_incident` (solo si no tiene dueño) y `comment_incident`. No hay "bloquear IP" ni ejecutar comandos a propósito: Kali es un sensor pasivo y bloquear requiere al firewall y una decisión humana (el puente correcto es un webhook hacia la automatización del firewall o un ticket). Salvaguardas: enfriamiento por playbook y entidad, tope de 60 acciones cada 10 min, corte de 5 min tras 3 fallos seguidos de webhook, `dry_run` por playbook o global (`SOC_PLAYBOOKS_DRY_RUN=1`), solo `https://` (salvo `allow_http`), y todo se registra en `data/playbook_log.jsonl`. `python3 scripts/playbooks.py --check | --test <id_alerta> | --runs`. Endpoints de lectura: `GET /api/playbooks` y `GET /api/playbooks/runs`.
- [ ] **Para activar un webhook** (Teams, Slack, etc.): crear `/etc/sentinel-soc/webhooks.env` (con sudo, modo 640, grupo `soc`) con una línea `SOC_WEBHOOK_URL=https://...` (la URL es un secreto y no va en git), poner `enabled: true` en el playbook, y probar primero con `"dry_run": true`. `format: "text"` envía `{"text","content"}` (Slack/Mattermost/Discord); Teams puede requerir otro formato y habría que ajustarlo.
- [ ] **Requiere reiniciar** `soc-api`, `soc-zeek-forwarder`, `soc-suricata-forwarder`, `soc-osquery-forwarder`, `soc-dhcp-fingerprint`, `soc-traffic-monitor` y `soc-login` para cargar el código nuevo.
- [ ] **Decisión pendiente:** hoy cualquier llave de lectura puede usar `/api/search`, que expone DNS, HTTP y tráfico interno. Si Sixto y Tomás comparten llave con más gente, conviene limitar la búsqueda a llaves de escritura o a un rol nuevo.

## Plan futuro: llaves YubiKey (sin fecha, no urgente)
- [ ] El equipo usará llaves Yubico en algún momento para reforzar la seguridad. Aún no se implementa nada. Cuando llegue el momento, los puntos naturales son: inicio de sesión del dashboard con WebAuthn/FIDO2 (lo de Sixto), SSH al Kali con llaves `ed25519-sk`, y opcionalmente `sudo` con PAM U2F.
- [ ] Ojo con la atribución: el API registra al autor de cada cambio (incidentes, supresiones, comentarios) según la llave de API. Si el dashboard autentica a cada persona con YubiKey pero habla con el API usando una sola llave del servidor, todos los cambios saldrían con el mismo autor. Conviene decidir entonces si cada persona tiene su propia llave de API o si el API acepta el usuario real desde el frontend.
- Una YubiKey no protege por sí sola un token de API (es un secreto que se envía tal cual); protege el inicio de sesión de las personas.

## 2026-09-18 (noche) — menos ruido de puertos que "reaparecen"
- [x] El escaneo de las 16:02 (primero con rastreo por MAC) dio 36 alertas `NEW open port`, casi igual al promedio previo (~39 por escaneo): el cambio a MAC no bastaba. Causa que queda: puertos que desaparecen y vuelven entre escaneos (sobre todo `161/udp`, porque el barrido UDP a veces no recibe respuesta), que se contaban como abiertos "nuevos".
- [x] `nmap_to_alerts.py` ahora guarda por dispositivo cuándo vio cada puerto por última vez (`seen`, con retención de 14 días). Un puerto "nuevo" visto en las últimas `SOC_PORT_QUIET_HOURS` (24 por defecto) se registra en el log pero **no alerta**; uno nunca visto, o ausente por más tiempo, sí alerta. Aplica al TCP y al UDP (comparten la misma lógica). Un estado antiguo sin historial alerta como antes (valor seguro por defecto).
- [ ] El efecto empieza a notarse desde el segundo escaneo después del cambio (el primero solo estampa `seen`). Verificar con el escaneo de las 20:02 y el de las 00:02: el número de `NEW open port` debe bajar de ~36 a la mitad o menos. Si no baja, revisar qué queda con `python3 scripts/log_search.py --source alerts --q 'title~"NEW open port"' --since 6h`.
- [ ] Sin commitear al escribir esto.

## 2026-09-21 — escaneo profundo del domingo y revisión de ruido
- [x] **El escaneo profundo semanal (domingo 20-sep 02:08) falló** en menos de un segundo y no escaneó nada: `3_vuln_scan.sh` pide confirmar autorización y `scheduled_vuln_scan.sh` (a diferencia de `scheduled_scan.sh`) no exportaba `SCAN_AUTHORIZED=yes`. Arreglado. Nunca se ha ejecutado desatendido; la primera corrida real será el domingo 27-sep 02:00 salvo que se lance a mano antes (coordinar con IT: incluye nikto y toda la categoría `vuln` de nmap).
- [x] **Mejora de puertos confirmada:** tras el cambio del 18-sep, `NEW open port` pasó de ~39 alertas por escaneo a ~5.
- [x] **Vulnerabilidades repetidas:** 23 hallazgos distintos habían generado 130 alertas (hasta 12 para el mismo certificado vencido), porque un hallazgo ausente un solo escaneo se olvidaba y volvía a alertar como nuevo. Ahora se recuerda `VULN_GONE_HOURS` (72 h) antes de decir "No longer detected".
- [x] `traffic_to_alerts.py`: el tráfico del propio Kali necesita el doble de puertos para contarse como escaneo.
- [x] `aide_to_alerts.py`: un cambio en un archivo de este proyecto que coincide con su último commit baja a `normal` y anota el commit; si difiere de git sigue `critical`.
- [ ] Pendiente con `sudo` (no lo puede hacer Claude): excluir de AIDE `/var/lib/apt`, `/var/lib/PackageKit`, `/var/lib/rpm`, `/var/lib/command-not-found`, `/var/lib/aide/aide.db.new` y `/opt/sentinel-soc/.*/__pycache__` en `/etc/aide/aide.conf.d/90_sentinel_soc`, y rehacer la base con `aide --init` (~30 min; escribe directo a `aide.db`, respaldar antes).
- [x] **Limpieza hecha (aprobada por el usuario, 2026-09-21):** 125 alertas resueltas con nota (`actor: claude-code`): 45 duplicados de vulnerabilidades (queda una abierta por hallazgo), 52 "No longer detected" que luego volvieron y 28 cambios de AIDE que coinciden con su último commit. Alertas abiertas: 434 → 309. Copia previa del feed en el scratchpad de la sesión.
- [x] Commiteado el 2026-09-21.

## 2026-09-21 — regla de "huella no confirmada" (nmap_to_alerts.py)
- [x] Antes: un puerto nuevo cuyo servicio nmap solo pudo adivinar por el número (`method == table`) subía un nivel de severidad **siempre**. Nació de un caso real (equipo IoT con un puerto que no respondía como su protocolo, 2026-09-11) pero generaba críticas por servicios normales: la iDRAC de Dell (VNC sobre TLS), un NETGEAR (4242), un teléfono IP (SIP-TLS).
- [x] Ahora sube de nivel **solo si un equipo ya establecido (presente en el estado anterior) abre un puerto que nunca había mostrado** (no está en su historial `seen` de 14 días). En un equipo recién aparecido, que ya tiene su alerta de dispositivo nuevo, o en un puerto que el equipo ya tuvo, la alerta conserva "— unconfirmed service fingerprint" en el título pero mantiene su severidad base; `details.unconfirmed_escalated` indica si subió. Un estado antiguo sin historial sube como antes (valor seguro). La ruta manual sin `--diff-state` no cambia.
- [ ] Efecto esperado: menos críticas por dispositivos nuevos. Verificar en 2-3 días con `python3 scripts/log_search.py --source alerts --q 'title~unconfirmed severity:critical' --since 3d`. Sin commitear.

## 2026-09-21 — escaneo profundo verificado y ruido de "dispositivo nuevo"
- [x] **Escaneo profundo del domingo:** la causa del fallo (falta de `SCAN_AUTHORIZED=yes`) ya estaba corregida. Se verificó ejecutando `3_vuln_scan.sh` sin terminal contra `127.0.0.1`: pasó la autorización, usó `sudo nmap` sin contraseña y arrancó normal (se detuvo a los 270 s a propósito; con ~300 equipos durará varias horas). Todas las herramientas están instaladas. La primera corrida real desatendida será el domingo 27-sep 02:00; revisar el lunes 28 los resultados y el estado con `systemctl show soc-vuln-scan.service -p Result`. Nota: desde una terminal sin el grupo `soc` aplicado hace falta `sg soc -c` para ejecutarlo a mano.
- [x] **Dispositivos nuevos:** `arp_to_alerts.py` agrupa las oleadas: más de `SOC_ARP_BULK_THRESHOLD` (5) equipos nuevos del mismo tipo en una VLAN en un escaneo salen como **una** alerta "N new device(s) on VLAN ... in one scan" con la lista en `details.devices` y las entidades de cada equipo; hasta 5 siguen siendo alertas individuales. Las MAC aleatorias se agrupan aparte (severidad normal). La VLAN Wiping pasa a severidad `normal` (su rotación es el estado normal). La regla de correlación `new-device-then-scan` y las etiquetas MITRE (T1200) reconocen el título agrupado.
- [x] Limpieza en el feed (2026-09-21): 43 alertas de dispositivo nuevo resueltas (14 de Wiping, 29 de MAC aleatoria). Quedan abiertas 33 de equipos reales de Floor e Impresoras, pendientes de confirmar con IT.

## 2026-09-21 (tarde) — ruido, respaldo, tareas con sudo y prueba de humo
- [x] **Reinicio inesperado a las 16:03:** apagado limpio (`System Power Off`) y arranque de la máquina virtual, iniciado desde fuera (probablemente Proxmox); cortó la sesión de trabajo y el escaneo de las 16:00, que dejó una marca colgada (limpiada). Todos los servicios volvieron y cargaron el código nuevo. Preguntar a IT si fue intencional; es otra pista para los cortes de SSH (capa de Proxmox).
- [x] **Ruido (pasada sobre las medias):** `login_monitor` alerta una sola vez cada 24 h por IP y usuario para "Login from NEW IP" (`SOC_NEW_IP_COOLDOWN`); 3 reglas de supresión versionadas con caducidad (`zeek-self-signed-internal`, `aide-package-manager-paths` y `aide-project-bytecode`, esta última con `allow_critical` y una regex estrecha que solo cubre `__pycache__/*.pyc` y los directorios del proyecto, nunca archivos fuente); **envejecimiento de alertas** (`scripts/alert_aging.py`, `config/alert_aging.json`): cierra solas, tras 2-3 días sin revisar, las alertas informativas de cambio (puertos nuevos, "No longer detected", MAC aleatorias, logins rutinarios, recuperaciones de servicio). Nunca toca críticas, AIDE, hallazgos de vulnerabilidades, dispositivos reales, alertas reconocidas ni ligadas a un incidente; lo ejecuta el watchdog como máximo cada 6 h; cada cierre queda con nota y actor `auto-aging`. Limpieza puntual: 19 hallazgos del reporte de IT reconocidos (`acknowledged`) y 30 alertas explicadas resueltas. Abiertas sin revisar: 261 → 164.
- [x] **Respaldo diario** (`kali/backup_data.sh`, cron 03:17 de tu usuario): `data/` y `config/` sin las llaves del API ni los feeds regenerables, retención de 14, verificación de integridad, mode 600. Hoy solo hay copia local en `~/soc-backups/`; falta un destino remoto (`SOC_BACKUP_REMOTE=usuario@host:/ruta`, con `SOC_BACKUP_GPG_RECIPIENT` para cifrar) que debe dar IT. Restaurar para inspeccionar: `kali/backup_data.sh --restore ARCHIVO DIRECTORIO` (nunca escribe sobre `data/`). Si se restaura, hay que reemitir las llaves del API. Vigilado por `source_health` (alerta si pasan 26 h sin respaldo) y por el doctor.
- [ ] **Pendiente tuyo, con sudo:** `sudo bash /opt/sentinel-soc/deploy/sudo_tasks.sh` añade las exclusiones de AIDE (gestor de paquetes y `__pycache__`), respaldando y validando la configuración; con `--reinit-aide` rehace la base (~30 min). Las reglas de supresión provisionales se pueden retirar después. `sysstat` ya recolecta y `adelcueto` ya está en `adm`, así que no había nada más que hacer con sudo para el diagnóstico de SSH.
- [x] **Prueba de humo** (`scripts/soc_selftest.py`): alimenta eventos sintéticos e inofensivos (IPs de documentación, la cadena de prueba EICAR, logins inventados) por el código real de cada detector y comprueba alertas, etiquetas MITRE, incidentes, supresión, envejecimiento, búsqueda, respaldo y API en un directorio temporal (65 comprobaciones, ~2 s). `--live` añade lectura de servicios, timers, doctor, salud de fuentes, respaldo, feeds, disco y API real (21 más); `--canary` manda una alerta real "selftest" por el flujo vivo y la resuelve. Cron diario 06:30 con `--live --alert-on-fail` (si algo falla, aparece una alerta media "SOC self-test failed"). El doctor avisa si la última corrida falló o tiene más de 48 h. Útil también para demostrar la detección en la certificación del 29 de octubre.
- [x] Encontrado con la prueba de humo: el detector de tráfico exige ver una conexión en claro al menos dos veces (para no confundir un sondeo con una sesión); un solo SYN a Telnet no alerta, por diseño.
- [x] **Complementos (sesión principal):** (a) *Envejecimiento de alertas* (`scripts/alert_aging.py`, reglas en `config/alert_aging.json`, lo dispara el watchdog como máximo cada 6 h): cierra sola, con nota y actor `auto-aging`, la alerta informativa que lleva 2-3 días sin revisar (puertos nuevos, "no longer detected", MAC aleatorias, logins normales, servicios recuperados); nunca toca críticas, AIDE, hallazgos de vulnerabilidades, dispositivos reales, alertas reconocidas ni las ligadas a un incidente. (b) Los 19 hallazgos del reporte de IT quedaron `acknowledged` y 30 alertas explicadas se resolvieron: abiertas sin revisar 261 → 164. (c) `soc_selftest.py`: 35 pruebas aisladas (un directorio temporal, sin notificaciones) + 30 de solo lectura del sistema vivo + `--canary` (una alerta inofensiva por el pipeline real que se resuelve sola); se verificó que falla de verdad al romper algo a propósito. Con `--alert-on-fail` (lo que corre el cron de las 06:30) genera **una** alerta cuando cambia el conjunto de fallos y otra al recuperarse; escribe `data/selftest_last.json` como `{at, passed, total, failed, failures:[{name, detail}]}`, que es lo que lee `soc_doctor`. No cubre chkrootkit, disk_space, system_updates, vlan_segmentation, whatweb ni nikto. (d) `deploy/sudo_tasks.sh`: ya **no** hace falta dar permisos de logs ni arreglar `sysstat`: `adelcueto` ya está en `adm` (solo abrir sesión nueva) y `sysstat` recolecta desde el 16-sep. (e) El respaldo se probó también con cifrado GPG (llave desechable) y con destino remoto.
- [x] **Reinicio de las 16:0x:** el `journal` muestra **dos** apagados limpios seguidos (16:01:17 y 16:02:37, con un arranque de solo 46 s en medio) y el arranque definitivo a las 16:03:09: encaja con el error "Can't reach the API server (EAI_AGAIN)" de esa hora. Todos los servicios volvieron solos (comprobado por la prueba de humo). Falta saber **quién lo inició** (Proxmox/IT).
- [x] `~/bin/cc` (fuera del repo): abre Claude Code dentro de tmux para sobrevivir cortes de red/SSH; `cc` reconecta o muestra la lista de conversaciones tras un reinicio, `cc -c` reanuda la última, `cc -n` abre una nueva.
- [x] **Vigilante de DNS/red** (`scripts/dns_probe.py`, cron cada minuto del usuario `adelcueto`, registro en `data/dns_probe.jsonl`, 14 días): en cada corrida hace una consulta real de `api.anthropic.com` a **cada** DNS de `/etc/resolv.conf` (hoy `10.69.0.14` y `10.69.0.15`), una por el resolvedor del sistema (el camino de las aplicaciones) y un ping al gateway; una corrida no se apila sobre otra. `python3 scripts/dns_probe.py --report [HORAS]` agrupa los fallos en **episodios** con hora de inicio y fin y qué objetivo falló, y muestra los **huecos sin datos**, que son reinicios o apagados de la máquina. Sirve para comparar con los cortes de SSH y darle a IT (Proxmox) evidencia con hora. No genera alertas.

## 2026-09-21 (noche) — los 6 pedidos de Sixto para la Kali
- [x] **1. Severidad y agrupación de `aide` y `kali_scan`** (34,900 de 39,118 alertas). *Puertos (`kali_scan`):* un puerto nuevo detectado por nuestro propio escaneo es `normal`; sube a `medium` solo si es un puerto de acceso remoto, archivos compartidos o base de datos (`HIGH_RISK_PORTS`) que se abre en un equipo **ya conocido** que nunca lo había mostrado. `critical` solo lo alcanza la regla de "huella no confirmada" (equipo establecido, nmap no pudo confirmar el servicio). Un certificado vencido o autofirmado es `normal`; `ftp-anon` es `medium`. Más de 4 puertos nuevos en un mismo equipo salen como **una** alerta "NEW open ports on IP: 22/tcp ssh, ..." con `details.ports[]` (`port, proto, service, banner, severity`) y `details.count`. Umbral: `SOC_HOST_BULK_THRESHOLD`. *AIDE:* por omisión `normal` (un archivo modificado en el equipo del SOC no es medio salvo motivo concreto); `medium` en `/etc/`, `/root/`, `/usr/local/`, unidades systemd y cualquier `/.ssh/`; `critical` solo en persistencia/escalada (`/etc/passwd`, `/etc/shadow`, `sudoers`, `sshd_config`, cron, `/var/spool/cron`, binarios del sistema, `/boot`, código del propio SOC), y si el archivo coincide con su último commit baja a `normal`. Más de 5 cambios del mismo tipo y severidad bajo la misma carpeta (3 niveles) salen como **una** alerta "File integrity: N files changed under DIR" con `details.files[:100]`, `details.count`, `details.dir`, `details.truncated` (`SOC_AIDE_BULK_THRESHOLD`). Lo que una regla de supresión ocultaría sigue saliendo individual. Nueva regla de envejecimiento `aide-normal` (3 días).
- [x] **2. Anonymizer en todas las IPs externas.** Antes solo se revisaba `source_ip`; el resto (IP remota en `details.dst`, `ioc_ip`...) quedaba sin bloque. Ahora se revisa `source_ip` si es pública y, si no, la primera IP pública que aparezca en `details`; `details.anonymizer_ip` dice a cuál IP describe el bloque. **Límite real:** no tenemos la base `GeoLite2-ASN.mmdb` (requiere licencia MaxMind), así que `tor` es confiable (lista de salidas, se refresca a diario) pero `datacenter`/`type` salen `false`/`unknown` para todo lo que no es Tor. Las alertas ya guardadas en el backend no se reescriben.
- [x] **3. Campo `test`.** `Alert(test=True)` o `detector` en (`manual_test`, `test`, `debug_test`) o `SOC_TEST_ALERTS=1`; los scripts `--demo` y `seed_demo_data.py` lo activan solos. El registro lleva `"test": true` y la clave **no existe** en las alertas normales. Una alerta de prueba **sí** se guarda y se reenvía al backend (para que aparezca en su pestaña Test), pero no abre incidentes, no ejecuta playbooks y no manda notificación push. Prueba: `python3 scripts/soc_core.py --test-alert`.
- [x] **4. Qué significa `source_ip`.** Nuevo `details.source_role`: `actor` = quien causa la actividad (escáner, atacante, origen del login); `asset` = el equipo del que trata la alerta (escaneado, monitoreado o un equipo interno cuyo tráfico se marcó; el otro extremo va en `details.dst`/`ioc_ip`). `traffic_capture`: port scan = `actor`; protocolo en claro = `asset` (el cliente; servidor en `details.dst`); IP maliciosa conocida = `asset`, o `actor` si la IP mala es la que originó el contacto; DNS sospechoso = `asset`. Por detector: `login_monitor` = actor; `kali_scan`, `arp_discovery`, `nikto`, `whatweb`, `osquery`, `vlan_segmentation` = asset; `suricata`/`zeek` = asset si un equipo interno habla hacia Internet, actor en los demás casos. Corregido de paso: en "protocolo en claro" el `hostname` era el del destino; ahora es el de `source_ip` y el del destino va en `details.dst_hostname`.
- [x] **5. IP 10.201.5.155.** **No es una IP anterior: es la IP actual de la Kali** (`eth1`, VLAN de Management, MAC `bc:24:11:9d:e7:95`, prefijo de Proxmox). Evidencia: es hoy la dirección de `eth1`; desde esa IP salen a diario las consultas DNS a `check.torproject.org` (nuestro refresco de la lista Tor) hasta el 17-sep; y el "barrido" del 12-sep 14:31–14:44 (hora local) fue de la propia Kali contra el gateway `10.201.0.1`: escaneo de 1,005 puertos, y luego la ronda de whatweb/nikto (15:04–15:05, `kali/results/nikto_20260912_150317.json`), cuyas rutas de prueba coinciden con firmas de Cisco/F5/Citrix/Fortinet/ColdFusion/D-Link. Total: 32 alertas con esa IP en el archivo (17 de Suricata de exploits, 9 de tráfico, 6 de Tor-checker). No hay nada que investigar del lado de la red; solo que en ese momento el escaneo era manual y `scan_active()` no lo cubría. Para que el dashboard las excluya y limpie las 19 marcas: nuevo `GET /api/self` (direcciones propias) y `details.source_is_self: true` en cada alerta nueva cuyo `source_ip` sea de la Kali. **Falta reiniciar `soc-api`** (sudo, lo hace el usuario) para que `/api/self` exista.
- [x] **6. Mantener lo que está bien:** no se tocó (un `detector` distinto por fuente, `source_ip` siempre presente donde aplica). Las alertas de prueba sin marca ya no llegan a producción porque los `--demo` marcan `test` solos.
- [x] **Efecto confirmado (2026-09-22, un día después):** `kali_scan` bajó de 20 medium/1 critical (20-sep) a 3 medium/0 critical (22-sep). `aide` subió de 6 a 14, pero es por el propio trabajo de hoy (reinicio de la base de AIDE, un evento único, más varios commits y cambios de crontab), no una regresión — de esas 14, 3 son las críticas ya revisadas y resueltas con nota. Selftest ya actualizado y commiteado (`04ec7d2`). Falta solo confirmar del lado de Sixto que el dashboard ya muestra `test`, `source_role`, `anonymizer_ip`, `source_is_self`.

## 2026-09-21 (noche) — pestaña Test del dashboard
- [x] `python3 scripts/soc_core.py --test-alert` ahora acepta `--severity`, `--type`, `--title`, `--source-ip`, `--detector`, `--details '{json}'` y `--count N` (títulos "(i of N)"); `--matrix` manda una alerta por cada tipo × severidad (15). Todas salen con `test: true`, no abren incidente, no ejecutan playbooks y no mandan push; sí se reenvían al backend para la pestaña Test.
- [ ] La alerta canario de `soc_selftest.py --canary` aún no lleva `test=True` (ya pedido a la otra sesión, dueña del archivo). No se marca `selftest` como detector de prueba porque ese mismo detector manda la alerta real "SOC self-test failed".
- [ ] 7 alertas de prueba antiguas (5 `manual_test`, 2 canarios) quedaron sin marca en el archivo local; las que ya están en el backend hay que ocultarlas o borrarlas allá.

## 2026-09-22 — catálogo de reglas de detección (GET /api/detections)
- [x] El endpoint ya existía; se amplió para que sirva como el equivalente del catálogo de "Analytics rules" de Sentinel. Cada detector en `detectors[]` ahora trae: `mitre` (técnicas que puede etiquetar), `by_severity_30d`, `last_alert_at`, y `tunables` (umbrales reales, leídos en vivo de las constantes del propio script — no copiados a mano, para que no se desactualicen si alguien ajusta una variable de entorno o un valor por omisión).
- [x] Cada regla de supresión en `suppression_rules[]` ahora trae también `match` (el criterio real que oculta la alerta), `allow_critical` y `source`, no solo el `id` y la razón.
- [x] Nueva función `mitre_tags.detector_techniques()` (inversa de `coverage_map()`).
- [x] Probado en aislado y con datos reales; `soc_selftest.py --isolated` sigue en verde (89/89).
- [ ] Pendiente (frontend, Sixto): esto es solo API; una vista tipo "catálogo de reglas" en el dashboard es tarea suya cuando tenga tiempo.

## 2026-09-22 — reinicio de AIDE, limpieza de críticas, falso positivo de tráfico
- [x] **AIDE reactivado de verdad:** `deploy/sudo_tasks.sh --reinit-aide` (corrido ayer por el usuario) escribe la base nueva en `aide.db.new`, no en `aide.db` como decía el comentario del script; sin moverla, las exclusiones nunca entraban en efecto. El usuario hizo el `mv` a mano; corregido el script para que lo haga solo (commit `9ac79c7`). Primer chequeo con la base nueva: 17 hallazgos, todos reales o cubiertos por una regla, contra las decenas de miles de antes.
- [x] **Regla de supresión de paquetes retirada, la de bytecode se vuelve permanente** (commit `8a4fa8d`): `aide-package-manager-paths` nunca se disparó (los excludes del config ya cubren eso). `aide-project-bytecode` sí sigue disparando: aunque el `.pyc` esté excluido, crear/borrarlo cambia la fecha de modificación de la carpeta padre (`kali`, `scripts`, `docs`, `config`), que AIDE sí sigue vigilando. No es ruido residual temporal, es un efecto estructural de AIDE — se queda.
- [x] **Falso positivo real corregido:** `10.69.0.14` (uno de nuestros DNS, `/etc/resolv.conf`) se marcaba como escaneo crítico cada hora desde el 21-sep 22:24, tocando 37-69 "puertos". Los puertos eran todos del rango efímero de Linux (32768-60999): son respuestas normales a nuestras propias consultas DNS (`dns_probe.py` corre cada minuto, más resolución de hostnames de rutina), no el DNS escaneándonos. Un escaneo real apunta a puertos de servicio, nunca al rango efímero. Corregido en `kali/traffic_to_alerts.py` excluyendo el rango efímero del conteo de puertos destino (commit `dfd045a`), con prueba de regresión en `soc_selftest.py`. El contador de SYN-sin-ACK (la otra vía de detección) no se tocó.
- [x] **Limpieza del feed:** 25 alertas del falso positivo del DNS + 4 críticas ya explicadas (2 de crontab por cambios propios de esta semana, `sudo_tasks.sh` ya commiteado, y un parpadeo transitorio de `soc-zeek-forwarder` — el servicio lleva corriendo sin cortes desde antes de esa alerta) resueltas con nota (`actor: claude-code`). Abiertas: 201 → 172.
- [x] **Revisadas las 4 vulnerabilidades críticas restantes — todas reales, ninguna es ruido:** `192.168.7.212` (Office) y `192.168.61.12` (Printer Guys) son impresoras HP con FTP anónimo de fábrica (JetDirect, confirmado por el listado de directorio en la segunda); `192.168.61.20` (Printer Guys) es una Zebra con el mismo problema; `10.21.0.111` (Wiping) es un Dell con VNC nuevo sin confirmar, casi seguro la consola de la iDRAC ya pendiente de confirmar con IT. Ninguna se puede corregir desde Kali (Fase 1, solo detección pasiva); quedaron `acknowledged` con nota (fabricante, VLAN, por qué es real) para ir en el mismo paquete a IT que el hallazgo de segmentación de VLANs — con la segmentación rota, las 4 son alcanzables desde cualquier VLAN, no solo la suya. Críticas abiertas: 26 → 0.
- [ ] Pendiente del usuario: mandar a IT el paquete de segmentación de VLANs + estas 4 (impresoras: desactivar FTP anónimo; Dell: confirmar iDRAC y restringir a Management).

## 2026-09-22 (tarde) — watchlists formales
- [x] Nuevo `scripts/watchlists.py`: junta las listas de confianza que antes vivían sueltas (`data/known_ips.txt`, `kali/ioc_ips.txt`, `kali/bad_domains.txt`, `data/ioc_hashes.txt`) más las dos VLAN (`sensitive_vlans`, `untrusted_vlans`, antes hardcodeadas en `vlan_segmentation_to_alerts.py`) en un solo lugar con lectura/escritura consistente. Cada watchlist valida su tipo de entrada (IP/CIDR, dominio, hash md5/sha1/sha256, nombre de VLAN) y queda auditada en `data/watchlist_log.jsonl` (quién, qué, cuándo).
- [x] Los detectores no cambiaron cómo leen sus archivos (siguen siendo los mismos `.txt`); `watchlists.py` es una capa encima para administrarlos sin editar archivos a mano, con bloqueo y escritura atómica que preserva el encabezado de comentarios de cada archivo.
- [x] `vlan_segmentation_to_alerts.py` ahora lee `SENSITIVE_VLANS`/`UNTRUSTED_VLANS` de las watchlists en vez de un valor fijo en el código; sembró los mismos dos valores que tenía antes (Management/Wiping y Guest-Employee-WiFi), así que no hay cambio de comportamiento.
- [x] Nuevos endpoints: `GET /api/watchlists`, `GET /api/watchlists/<nombre>`, `POST /api/watchlists/<nombre>` (agregar, cuerpo `{"entry": "..."}`), `DELETE /api/watchlists/<nombre>?entry=...` — solo con llave de escritura.
- [x] Probado en aislado (validación, duplicados, borrado, permisos de lectura/escritura) y contra un servidor de API real en un directorio temporal; `soc_selftest.py --isolated` sigue en 89/89.
- [ ] Pendiente (frontend, Sixto): una vista para administrar las watchlists desde el dashboard, cuando tenga tiempo.

## 2026-09-22 (tarde) — endpoint de descarga de pcap
- [x] Nuevo `GET /api/alerts/<id>/pcap`: descarga la captura completa (`details.pcap`) de una alerta de `traffic_capture`, para abrirla en Wireshark fuera de la Kali.
- [x] Por seguridad, el endpoint recibe el **id de la alerta**, nunca una ruta; internamente resuelve `details.pcap` y confirma que el archivo real quede dentro de `kali/results/flagged_captures/` antes de servirlo — así un llamador no puede pedir un archivo arbitrario del equipo. Probado con un intento de path traversal (archivo fuera del directorio de capturas): rechazado con 404, igual que un archivo que no existe o una alerta sin captura.
- [x] Requiere llave de API válida (lectura o escritura); sin autenticación, 401.
- [x] Archivo servido en streaming (hasta 42 MB hoy), no se carga completo en memoria.
- [x] Probado con datos reales contra un servidor de API aislado; `soc_selftest.py --isolated` sigue en 89/89.
- [ ] Pendiente (frontend, Sixto): botón de descarga en el dashboard cuando `details.pcap` esté presente, apuntando a este endpoint — ya no hace falta ocultarlo como se dijo antes, ahora sí es descargable.

## Idea futura (post-certificación 29-oct) — mapa interactivo de alertas
- [ ] **Idea del usuario:** un mapa mundial en el dashboard donde cada alerta aparezca como un punto parpadeando en el lugar de origen (estilo mapa de ciberataques en vivo). Explícitamente pospuesto por el usuario hasta después de la certificación del 29 de octubre; no implementar antes.
- [x] **Diagnóstico de viabilidad (2026-09-22):** el lado de Kali ya está casi listo y dormido desde el primer commit del proyecto. `scripts/geoip_enrich.py` existe desde el día uno, ya enganchado a `emit_alert()`, y produce exactamente `details.geo = {"country", "country_code", "city", "lat", "lon"}` para cualquier IP pública en una alerta. Nunca se activó porque falta el archivo `data/GeoLite2-City.mmdb` (cuenta gratis en MaxMind, "GeoLite2 City", no la de ASN que ya sabíamos que faltaba). Confirmado: 0 alertas traen `details.geo` hoy.
- [x] **Expectativa realista, importante para no prometer de más:** de las últimas 231 alertas (~2.5 días), solo 6 (≈3%) tienen alguna IP pública en algún lado (`source_ip` o `details`). Es un SOC de red interna (6 VLANs privadas), no un honeypot expuesto a Internet, así que el mapa parpadearía ocasionalmente (coincidencias de threat intel, remitentes de phishing, intentos de login externo), no de forma constante como los mapas tipo Kaspersky/Norse — que se alimentan de honeypots públicos. Cada parpadeo sería una señal real, no relleno visual.
- [ ] **Repartición cuando se retome:** Kali — activar la base de datos (~5 min, sin tocar código). Sixto — la parte visual (ej. Leaflet) alimentada por `details.geo`, con polling o websocket sobre alertas nuevas.
